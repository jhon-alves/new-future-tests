import styled from 'styled-components/native';
import { LinearGradient } from 'expo-linear-gradient';

export const DrawerContainer = styled(LinearGradient)`
  flex: 1;
`;

export const DrawerHeader = styled.View`
  padding: 20px;
`;
